# Order Management System Submission

Sample candidate submission for the TechDose AI coding assessment.

The implementation focuses on:

- Explicit order state transitions
- Thread-safe order storage
- Unique order IDs under concurrent creation
- Atomic cancellation and shipping decisions
- A small concurrency test suite

The required agent transcript is in `agent-log.md`.
